export const metadata = {
  title: "Civifit | Login/Signup",
  description: "Login/Signup civifit",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <main>{children}</main>;
}
